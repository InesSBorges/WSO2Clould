openapi: 3.0.1
info:
  title: VRS_VerticalSign
  description: API Definition of VRS_VerticalSign
  version: 1.0.0
servers:
- url: /VRS_VerticalSign
paths:
  /getVerticalSign:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
  /verticalSign:
    get:
      responses:
        default:
          description: Default response
  /setVerticalSing:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
